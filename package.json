{
  "name": "velos",
  "version": "0.1.0",
  "description": "Financial control layer for AI agent spending — policy engine that approves, denies, or escalates spending requests",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
